image: node:14.17.2

options: 
  docker: true

pipelines:
  branches:
    validate-tockens:
      - step:
          script:
            - docker-compose -v
            - docker-compose up
          services:
          - docker

