image: node:14.17.2

pipelines:
  branches:
    validate-tockens:
      - step:
          script:
            - docker-compose -v
            - docker-compose up
          services:
          - docker

definitions:
    services:
        docker:
            image: docker/compose:1.25.4